<h1>つぶやき一覧</h1>
<% @rubeets.each do |rubeet| %>
	<tr>
		<p>
			<td><%= rubeet.content %></td>
			<td><%= link_to "つぶやきを編集する",edit_rubeet_path(rubeet.id) %></td>
		</p>
	</tr>
	<% end %>
</table>
<%= link_to '新しくつぶやく',new_rubeet_path %> 

